import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eyyzamero.logistics.helper',
  appName: 'LogisticsHelper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;