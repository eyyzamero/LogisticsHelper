import { IonProgressBarColorDirective } from './ion-progress-bar-color.directive';

describe('IonProgressBarColorDirective', () => {
  it('should create an instance', () => {
    const directive = new IonProgressBarColorDirective();
    expect(directive).toBeTruthy();
  });
});
